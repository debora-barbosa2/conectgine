<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastro de Usuária - ConectGine</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
  --primary-color: rgba(214, 51, 132, 0.8);/* Rosa forte */
  --primary-color-light: #f9d4e7;
  --secondary-color: rgba(214, 51, 132, 0.8);
  --error-color: #f44336;
  --success-color: #4caf50;
  --input-bg: #fff;
  --input-border: #ccc;
  --input-border-focus: var(--primary-color);
  --font-color: #333;
  --font-color-light: #777;
}

* {
  box-sizing: border-box;
}

body {
  font-family: 'Montserrat', sans-serif;
  background: linear-gradient(135deg, #f9e3f0 0%, #f5f5f5 100%);
  margin: 0;
  padding: 0;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

.cadastro-usuario-container {
  background-color: var(--input-bg);
  border-radius: 12px;
  box-shadow: 0 10px 30px rgba(214, 51, 132, 0.2);
  padding: 3rem 2.5rem 3rem 2.5rem;
  max-width: 480px;
  width: 90%;
  animation: fadeIn 0.6s ease forwards;
}

h1 {
  color: var(--primary-color);
  font-weight: 700;
  text-align: center;
  margin-bottom: 1.8rem;
  font-size: 1.9rem;
  letter-spacing: 1px;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 0.6rem;
}

h1 i {
  font-size: 1.8rem;
}

.info-box {
  background-color: var(--primary-color-light);
  border-left: 6px solid var(--primary-color);
  padding: 1rem 1.2rem;
  border-radius: 8px;
  color: var(--primary-color);
  font-weight: 600;
  margin-bottom: 2rem;
  font-size: 0.95rem;
  line-height: 1.4;
}

form {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.input-group {
  display: flex;
  flex-direction: column;
}

label {
  font-weight: 600;
  color: var(--font-color);
  margin-bottom: 0.5rem;
  font-size: 0.95rem;
}

input[type="text"],
input[type="email"],
input[type="password"],
input[type="date"],
input[type="tel"],
select {
  padding: 12px 16px;
  font-size: 1rem;
  border: 1.8px solid var(--input-border);
  border-radius: 8px;
  background-color: var(--input-bg);
  color: var(--font-color);
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
  font-family: inherit;
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="password"]:focus,
input[type="date"]:focus,
input[type="tel"]:focus,
select:focus {
  border-color: var(--input-border-focus);
  box-shadow: 0 0 6px var(--primary-color);
  outline: none;
}

.input-error {
  border-color: var(--error-color) !important;
  box-shadow: 0 0 5px var(--error-color);
}

.error-message {
  color: var(--error-color);
  font-size: 0.85rem;
  margin-top: 0.3rem;
  display: none;
}

.success-message {
  color: var(--success-color);
  font-size: 0.85rem;
  margin-top: 0.3rem;
  display: none;
}
.password-container {
  position: relative;
  width: 100%;
}

.password-container input {
  width: 100%;
  padding-right: 40px; /* espaço para o ícone */
  box-sizing: border-box;
}

.toggle-password {
  position: absolute;
  right: 15px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--font-color-light);
  cursor: pointer;
  transition: color 0.3s ease;
  font-size: 1.1rem;
}

.toggle-password:hover {
  color: var(--primary-color);
}

.checkbox-group {
  display: flex;
  align-items: flex-start;
  gap: 10px;
}

.checkbox-group input[type="checkbox"] {
  margin-top: 4px;
  width: 18px;
  height: 18px;
  cursor: pointer;
}

.checkbox-group label {
  font-weight: 500;
  font-size: 0.95rem;
  color: var(--font-color);
  user-select: none;
}

.checkbox-group a {
  color: var(--primary-color);
  text-decoration: none;
  font-weight: 600;
}

.checkbox-group a:hover {
  text-decoration: underline;
}

.form-actions {
  margin-top: 2rem;
  text-align: center;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.6rem;
  background-color: var(--primary-color);
  color: #fff;
  border: none;
  border-radius: 8px;
  padding: 14px 28px;
  font-size: 1.05rem;
  font-weight: 700;
  cursor: pointer;
  transition: background-color 0.3s ease;
  width: 100%;
  box-shadow: 0 5px 15px rgba(214, 51, 132, 0.3);
}

.btn:hover {
  background-color: var(--secondary-color);
  box-shadow: 0 7px 20px rgba(179, 45, 117, 0.5);
}

.login-link {
  margin-top: 1.8rem;
  text-align: center;
  font-size: 0.95rem;
  color: var(--font-color-light);
}

.login-link a {
  color: var(--primary-color);
  font-weight: 600;
  text-decoration: none;
}

.login-link a:hover {
  text-decoration: underline;
}

.google-btn {
  margin-top: 1.2rem;
  background-color: rgba(214, 51, 132, 0.8);
  font-weight: 600;
  box-shadow: 0 5px 15px rgba(219, 68, 55, 0.3);
}

.google-btn:hover {
  background-color: rgba(214, 51, 132, 0.8);
  box-shadow: 0 7px 20px rgba(194, 51, 33, 0.5);
}

.google-btn i {
  font-size: 1.2rem;
}

@media (max-width: 600px) {
  .cadastro-usuario-container {
    padding: 2rem 1.5rem;
    width: 95%;
    margin: 1rem;
  }

  .btn {
    padding: 12px 22px;
    font-size: 1rem;
  }
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px);}
  to { opacity: 1; transform: translateY(0);}
}

  </style>
</head>
<body>
  <div class="cadastro-usuario-container">
    <h1><i class="fas fa-user"></i> Cadastro de Usuária</h1>
    
    <div class="info-box">
      <p><strong>Importante:</strong> A ConectGine é uma plataforma exclusiva para mulheres. 
      Por favor, só prossiga com o cadastro se você se identifica como mulher.</p>
    </div>
    
    <form id="cadastroForm" action="/cadastro" method="POST">
      <div class="input-group">
        <label for="nome">Nome Completo</label>
        <input type="text" id="nome" name="nome" required>
        <span class="error-message" id="nome-error">Por favor, insira seu nome completo</span>
      </div>
      
      
      <div class="input-group">
        <label for="email">E-mail</label>
        <input type="email" id="email" name="email" required>
        <span class="error-message" id="email-error">Por favor, insira um e-mail válido</span>
      </div>
      
      <div class="input-group">
        <label for="senha">Senha</label>
        <div class="password-container">
          <input type="password" id="senha" name="senha" required minlength="8">
          <i class="fas fa-eye toggle-password" id="toggleSenha"></i>
        </div>
        <span class="error-message" id="senha-error">A senha deve ter pelo menos 8 caracteres</span>
      </div>
      
      <div class="input-group">
        <label for="confirma-senha">Confirmar Senha</label>
        <div class="password-container">
          <input type="password" id="confirma-senha" name="confirma-senha" required>
          <i class="fas fa-eye toggle-password" id="toggleConfirmaSenha"></i>
        </div>
        <span class="error-message" id="confirma-senha-error">As senhas não coincidem</span>
      </div>
      
      <div class="input-group">
        <label for="data-nascimento">Data de Nascimento</label>
        <input type="date" id="data-nascimento" name="data-nascimento" required>
        <span class="error-message" id="data-nascimento-error">Por favor, insira sua data de nascimento</span>
      </div>
      
      <div class="input-group">
        <span class="error-message" id="genero-error">Esta plataforma é exclusiva para mulheres</span>
      </div>
      
      <div class="input-group">
        <label for="telefone">Telefone</label>
        <input type="tel" id="telefone" name="telefone" placeholder="(00) 00000-0000" required>
        <span class="error-message" id="telefone-error">Por favor, insira um telefone válido</span>
      </div>
      
      <div class="checkbox-group">
        <input type="checkbox" id="termos" name="termos" required>
        <label for="termos">Declaro que me identifico como mulher e concordo com os 
          <a href="/termos" target="_blank">Termos de Uso</a> da plataforma</label>
        <span class="error-message" id="termos-error">Você deve confirmar sua identidade</span>
      </div>
      
      <div class="form-actions">
        <button type="submit" class="btn">
          <i class="fas fa-user-plus"></i> Cadastrar
        </button>
      </div>
    </form>
    
    <p class="login-link">Já possui cadastro? <a href="/login">Faça login aqui</a></p>
    <a href="/auth/google" class="btn google-btn">
          <i class="fab fa-google"></i> Entrar com Google
        </a>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Mostrar/ocultar senha
      document.getElementById('toggleSenha').addEventListener('click', function() {
        const senhaInput = document.getElementById('senha');
        const type = senhaInput.getAttribute('type') === 'password' ? 'text' : 'password';
        senhaInput.setAttribute('type', type);
        this.classList.toggle('fa-eye-slash');
      });
      
      document.getElementById('toggleConfirmaSenha').addEventListener('click', function() {
        const confirmaSenhaInput = document.getElementById('confirma-senha');
        const type = confirmaSenhaInput.getAttribute('type') === 'password' ? 'text' : 'password';
        confirmaSenhaInput.setAttribute('type', type);
        this.classList.toggle('fa-eye-slash');
      });
      
      // Máscara para telefone
      document.getElementById('telefone').addEventListener('input', function(e) {
        let value = e.target.value.replace(/\D/g, '');
        if (value.length > 2) {
          value = `(${value.substring(0, 2)}) ${value.substring(2)}`;
        }
        if (value.length > 10) {
          value = `${value.substring(0, 10)}-${value.substring(10, 15)}`;
        }
        e.target.value = value;
      });
      
      // Validação do formulário
      document.getElementById('cadastroForm').addEventListener('submit', function(e) {
        let isValid = true;
        const generosPermitidos = ['Mulher', 'Prefiro nao informar'];
        
        // Validação do nome
        const nome = document.getElementById('nome').value.trim();
        if (nome === '') {
          showError('nome-error', 'nome');
          isValid = false;
        } else {
          hideError('nome-error', 'nome');
        }
        
        // Validação do email
        const email = document.getElementById('email').value.trim();
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!emailRegex.test(email)) {
          showError('email-error', 'email');
          isValid = false;
        } else {
          hideError('email-error', 'email');
        }
        
        // Validação da senha
        const senha = document.getElementById('senha').value;
        if (senha.length < 8) {
          showError('senha-error', 'senha');
          isValid = false;
        } else {
          hideError('senha-error', 'senha');
        }
        
        // Validação de confirmação de senha
        const confirmaSenha = document.getElementById('confirma-senha').value;
        if (confirmaSenha !== senha) {
          showError('confirma-senha-error', 'confirma-senha');
          isValid = false;
        } else {
          hideError('confirma-senha-error', 'confirma-senha');
        }
        
        // Validação da data de nascimento
        const dataNascimento = document.getElementById('data-nascimento').value;
        if (dataNascimento === '') {
          showError('data-nascimento-error', 'data-nascimento');
          isValid = false;
        } else {
          hideError('data-nascimento-error', 'data-nascimento');
        }
        
        // Validação do gênero
        const genero = document.getElementById('genero').value;
        if (!generosPermitidos.includes(genero)) {
          showError('genero-error', 'genero');
          isValid = false;
        } else {
          hideError('genero-error', 'genero');
        }
        
        // Validação do telefone
        const telefone = document.getElementById('telefone').value.replace(/\D/g, '');
        if (telefone.length < 11) {
          showError('telefone-error', 'telefone');
          isValid = false;
        } else {
          hideError('telefone-error', 'telefone');
        }
        
        // Validação dos termos
        if (!document.getElementById('termos').checked) {
          showError('termos-error', 'termos');
          isValid = false;
        } else {
          hideError('termos-error', 'termos');
        }
        
        if (!isValid) {
          e.preventDefault();
          if (!generosPermitidos.includes(genero)) {
            alert('Esta plataforma é exclusiva para mulheres.');
          }
        }
      });
      
      function showError(errorId, inputId) {
        document.getElementById(errorId).style.display = 'block';
        if (inputId) {
          document.getElementById(inputId).classList.add('input-error');
        }
      }
      
      function hideError(errorId, inputId) {
        document.getElementById(errorId).style.display = 'none';
        if (inputId) {
          document.getElementById(inputId).classList.remove('input-error');
        }
      }
    });
  </script>
</body>
</html>
